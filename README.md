# First_ML_Project
Built a neural network from scratch in NumPy to classify handwritten alphabets (EMNIST). Implemented forward/backpropagation, ReLU, softmax, and mini-batch gradient descent. Achieved 86%+ test accuracy, demonstrating strong understanding of deep learning fundamentals and model optimization.
